server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_mental_helper?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: ************
    driver-class-name: com.mysql.cj.jdbc.Driver

  # ====== 完美融入：新增 AI 大模型占位配置，阻止启动闪退 ======

  ai:
    openai:
      api-key:
      base-url: https://api.deepseek.com
      chat:
        options:********************
          model: deepseek-v4-pro
          temperature: 0.7

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # 控制台打印 SQL
  global-config:
    db-config:
      id-type: auto        # 主键自增